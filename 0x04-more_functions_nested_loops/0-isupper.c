#include "main.h"

/**
 * _isupper - checking if c is upper
 * @c: input for alphabet
 * Return: if 1 uppercase. If 0 char is lowercase
 */

int _isupper(int c)
{
	int a;

	if (c >= 65 && c <= 90)
		return (1):
	else
		return (0);
}
